import React from 'react';
import ProductGrid from '../components/ProductGrid';

const Home = () => {
    return (
        <div className="p-4">
            <ProductGrid />
        </div>
    );
};

export default Home;
